type state = {
  "broadcaster-lang": string,
  "emote-only": bool,
  "followers-only": bool, // TODO string | bool
  "r9k": bool,
  "rituals": bool,
  "room-id": string,
  "slow": bool, // TODO string | bool
  "subs-only": bool,
  "channel": string,
}
